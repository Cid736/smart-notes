PORT=3001
# Required: set a long random string — the frontend uses it to authenticate API calls
# Generate one with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
API_KEY=change_me_generate_a_random_32_byte_hex_string
